const data=window.RESOURCE_DATA||[],grid=document.getElementById("resourceGrid"),input=document.getElementById("siteSearch"),none=document.getElementById("noResults");let active="all";function render(){const t=input.value.toLowerCase().trim();const rows=data.filter(r=>(!t||(r.title+" "+r.category+" "+r.summary+" "+r.links.flat().join(" ")).toLowerCase().includes(t))&&(active==="all"||r.category===active));grid.innerHTML=rows.map(r=>`<article class="resource-card" id="resource-${r.id}"><div class="resource-top"><div class="resource-icon">${r.icon}</div><span class="resource-category">${r.category}</span></div><h3>${r.title}</h3><p>${r.summary}</p><div class="resource-status">${r.status}</div><div class="resource-links"><a class="pdf-placeholder" href="#documents">PDF guide coming soon</a>${r.links.map(l=>`<a target="_blank" rel="noopener" href="${l[1]}">${l[0]} ↗</a>`).join("")}</div></article>`).join("");none.hidden=rows.length>0}render();input.addEventListener("input",render);document.getElementById("searchBtn").onclick=()=>{render();document.getElementById("resources").scrollIntoView({behavior:"smooth"})};document.querySelectorAll("[data-search]").forEach(b=>b.onclick=()=>{input.value=b.dataset.search;render();document.getElementById("resources").scrollIntoView({behavior:"smooth"})});document.querySelectorAll(".filter").forEach(b=>b.onclick=()=>{document.querySelectorAll(".filter").forEach(x=>x.classList.remove("active"));b.classList.add("active");active=b.dataset.filter;render()});const nav=document.getElementById("mainnav"),toggle=document.querySelector(".nav-toggle");toggle.onclick=()=>{const o=nav.classList.toggle("open");toggle.setAttribute("aria-expanded",o)};document.getElementById("documentGrid").innerHTML=data.slice(0,9).map(r=>`<div class="document-card"><span>${r.category.toUpperCase()}</span><strong>${r.title} Guide</strong><small>Placeholder ready for your PDF.</small></div>`).join("");